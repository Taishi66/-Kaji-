too-many-lines-threshold = 200

disallowed-methods = [
    { path = "uuid::Uuid::new_v4", reason = "boucle agent : passer par kaji::replay::idgen::IdGen (replay exact)" },
    { path = "chrono::Utc::now", reason = "boucle agent : passer par kaji::replay::clock::PromptClock (replay exact)" },
    { path = "kaji_provider_types::conversation::message::Message::with_generated_id", reason = "boucle agent : passer par kaji::replay::idgen::IdGen (replay exact)" },
]
