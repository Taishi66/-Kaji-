# kaji-sdk

Python bindings for the Kaji SDK.

This package is generated from the Rust `kaji-sdk` crate using UniFFI.

## Build a local wheel

From the repository root:

```bash
just --justfile crates/kaji-sdk/justfile python-wheel
```

The wheel is written to `crates/kaji-sdk/python/dist/`.
