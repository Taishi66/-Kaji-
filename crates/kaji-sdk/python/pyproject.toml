[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kaji-sdk"
version = "0.1.0a5"
description = "Python bindings for the Kaji SDK"
readme = "README.md"
requires-python = ">=3.9"
license = "Apache-2.0"
authors = [{ name = "AAIF", email = "ai-oss-tools@block.xyz" }]
keywords = ["kaji", "sdk", "ai", "agent", "uniffi"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Rust",
]

[project.urls]
Homepage = "https://github.com/Taishi66/-Kaji-"
Repository = "https://github.com/Taishi66/-Kaji-"
Issues = "https://github.com/Taishi66/-Kaji-/issues"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
kaji = ["*.so", "*.dylib", "*.dll", "py.typed"]
